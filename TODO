    User Management (app/modules/user_management.py):
        create_user(user_id, first_name, last_name=None, username=None): done
        get_user(connection, user_id)
        update_user_credit(connection, user_id, credit)
        update_user_status(connection, user_id, status)
        delete_user(connection, user_id)

    User Message Management (app/modules/message_processing.py):
        add_conversation(connection, user_id, message, sent_by):
        get_conversations(connection, user_id, limit=20)
        delete_conversations(connection, user_id, timestamp=None)

    User Activity Logs Management (app/modules/user_management.py):
        log_user_activity(connection, user_id, activity_type, tokens, duration)
        get_user_logs(connection, user_id, start_date=None, end_date=None, activity_type=None)
        delete_user_logs(connection, user_id, timestamp=None)